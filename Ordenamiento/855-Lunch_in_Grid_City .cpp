#include <iostream>
#include <vector>
#include <string>
#include <algorithm>

using namespace std;

int main() {
    int T;
    cin >> T;

    for (int caso = 1; caso <= T; ++caso) {
        int calle, avenida, amigo;
        cin >> calle >> avenida >> amigo;

        


    return 0;
}